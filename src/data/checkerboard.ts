import type { SpinnerDefinition } from './types';

export const checkerboard: SpinnerDefinition = {
  name: 'checkerboard',
  frames: ["⢕⢕⢕","⡪⡪⡪","⢊⠔⡡","⡡⢊⠔"],
  interval: 250,
  category: 'braille',
};
